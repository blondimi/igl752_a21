\documentclass{article}

%% Packages
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb}
\usepackage{enumerate}
\usepackage{multicol}
\usepackage{tikz}
\usetikzlibrary{automata, positioning}

%% LTL/CTL
\newcommand{\AP}{\mathit{AP}}
\newcommand{\true}{{\mathit{vrai}}}
\newcommand{\false}{{\mathit{faux}}}
\newcommand{\X}{{\mathsf{X}}}
\newcommand{\F}{{\mathsf{F}}}
\newcommand{\G}{{\mathsf{G}}}
\newcommand{\U}{\mathrel{\mathsf{U}}}
\newcommand{\sat}[1]{\llbracket#1 \rrbracket}

%% Automates et langages formels
\newcommand{\A}{\mathcal{A}}
\newcommand{\B}{\mathcal{B}}
\renewcommand{\L}{\mathcal{L}}
\newcommand{\T}{\mathcal{T}}

%% Autre
\newcommand{\avance}{{\large$\bigstar$}}

%% Titre
\title{IGL502/752: devoir 3}
\author{Foo McBar}
\date{2 novembre 2021}

\begin{document}
\maketitle

%% Contenu
\section*{Question 1}

\begin{multicols}{2}
  \begin{enumerate}[(a)]
    
  \item $\exists \F \forall \X \G \exists \X (p \lor q)$
    
  \item $\forall \X \neg \exists ((\exists \F p) \U \neg q)$
    
    
  \item $\forall \X \exists (q \land \forall \F p)$
    
  \item $((p \lor (\exists (p \U q))) \land (\exists \X \forall \G
    \forall \G p))$
  \end{enumerate}
\end{multicols}

\section*{Question 2}

\begin{enumerate}[(a)]

\item

  \begin{enumerate}[(i)]
  \item ...
  \item ...
  \item ...
  \item ...
  \item ...
  \end{enumerate}

\item ...

\end{enumerate}

\section*{Question 3}

\begin{enumerate}[(a)]
\item $\neg \forall (p \U q) \not\equiv \exists (p \U \neg q)$
  
\item $\exists \X \forall \G p \not\equiv \forall \X \exists \G p$
  
  
\item $\exists \F\, \forall \G\, \exists \F p \not\equiv \forall \G\,
  \exists \F p$

\end{enumerate}

\section*{Question 4}

\begin{center}
  \begin{tikzpicture}[auto, node distance=3cm, very thick, initial text=, transform shape, scale=0.88, font=\large]
    \node[state, initial,
          label={[xshift=-15pt, yshift=0pt]$\{p\}$}]     (s0) {$s_0$};
    \node[state, below of=s0, label={below:$\emptyset$}] (s1) {$s_1$};
    \node[state, right of=s0, label={$\{p, q\}$}]        (s2) {$s_2$};
    \node[state, left  of=s1, label={below:$\{p\}$}]     (s5) {$s_5$};
    \node[state, right of=s1, label={below:$\{q\}$}]     (s3) {$s_3$};
    \node[state, initial right,
          right of=s3, label={below:$\{p\}$}]            (s4) {$s_4$};
    
    \path[->]
    (s0) edge[loop above]   node {} ()
    (s0) edge[bend left=20] node {} (s1)
    (s0) edge[]             node {} (s2)
    (s1) edge[bend left=20] node {} (s0)
    (s1) edge[]             node {} (s2)
    (s1) edge[]             node {} (s3)
    (s1) edge[]             node {} (s5)
    (s2) edge[]             node {} (s3)
    (s2) edge[bend left=20] node {} (s4)
    (s3) edge[]             node {} (s4)
    (s4) edge[bend left=20] node {} (s2)
    (s4) edge[loop above]   node {} ()
    (s5) edge[loop above]   node {} ()
    ;
  \end{tikzpicture}
\end{center}

\begin{enumerate}[(a)]
\item $\forall \G (p \lor q)$
  
\item $\exists \G (p \lor q)$
  
\item $\exists (p \U q)$
  
\item $\exists \X\, \forall (p \U q)$

\item $\forall \G\, \exists \F\, \exists (p \U q)$
  
\end{enumerate}

\section*{Question 5}

\begin{center}
  \begin{tikzpicture}[auto, node distance=3cm, very thick, initial text=, transform shape, scale=0.88, font=\large]
    \node[state, initial,
          label={[xshift=-13pt, yshift=-3pt]$\emptyset$}] (s0) {$s_0$};
    \node[state, right of=s0, label={$\{p\}$}]            (s1) {$s_1$};
    \node[state, below of=s1, label={below:$\{p\}$}]      (s2) {$s_2$};
    \node[state, left  of=s2, label={below:$\{p, q\}$}]   (s3) {$s_3$};
    
    \node[above of=s0, xshift=-50pt, yshift=-65pt] {$\T_1:$};

    \path[->]
    (s0) edge[loop above]   node {} ()
    (s0) edge[]             node {} (s1)
    (s0) edge[bend left=20] node {} (s3)
    (s1) edge[loop right]   node {} ()
    (s1) edge[]             node {} (s2)
    (s2) edge[]             node {} (s3)
    (s3) edge[bend left]    node {} (s0)
    (s1) edge[]             node {} (s3)
    ;
  \end{tikzpicture}
  \begin{tikzpicture}[auto, node distance=3cm, very thick, initial text=, transform shape, scale=0.88, font=\large]
    \node[state, initial,
          label={[xshift=-13pt, yshift=-3pt]$\emptyset$}] (s0) {$s_0$};
    \node[state, right of=s0, label={$\{p\}$}]            (s1) {$s_1$};
    \node[state, below of=s1, label={below:$\{p\}$}]      (s2) {$s_2$};
    \node[state, left  of=s2, label={below:$\{p, q\}$}]   (s3) {$s_3$};
    
    \node[above of=s0, xshift=-50pt, yshift=-65pt] {$\T_2:$};

    \path[->]
    (s0) edge[loop above]   node {} ()
    (s0) edge[]             node {} (s1)
    (s0) edge[bend left=20] node {} (s3)
    (s1) edge[loop right]   node {} ()
    (s1) edge[]             node {} (s2)
    (s2) edge[]             node {} (s3)
    (s3) edge[bend left]    node {} (s0)
    ;
  \end{tikzpicture}
\end{center}

\section*{Question 6}

\begin{enumerate}[(a)]
\item $\forall \G \exists \F p$...

\item
  \begin{enumerate}[(i)]
  \item $\T_n \models \F\G p$ et $\T_n' \not\models \F\G p$...
    
  \item[\avance{}] ...

  \item ...
  \end{enumerate}
\end{enumerate}

\end{document}
