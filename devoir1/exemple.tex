\documentclass{article}

%% Packages
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{amsmath,amssymb}
\usepackage{enumerate}
\usepackage{tikz}
\usetikzlibrary{automata, positioning}

% Systèmes de transitions
\newcommand{\T}{\mathcal{T}}                   %% Système
\newcommand{\trans}[1]{\xrightarrow{#1}}       %% Relation de transition
\newcommand{\Pre}{\mathrm{Pre}}                %% Prédecesseurs immédiats
\newcommand{\Post}{\mathrm{Post}}              %% Successeurs immédiats
\newcommand{\AP}{\mathit{AP}}                  %% Propositions atomiques
\newcommand{\trace}[1]{\mathrm{trace}(#1)}     %% Trace d'une exécution
\newcommand{\Traces}[1]{\mathrm{Traces}(#1)}   %% Traces d'un système

%% LTL
\newcommand{\true}{{\mathit{vrai}}}
\newcommand{\false}{{\mathit{faux}}}
\newcommand{\X}{{\mathsf{X}}}
\newcommand{\F}{{\mathsf{F}}}
\newcommand{\G}{{\mathsf{G}}}
\newcommand{\U}{\mathrel{\mathsf{U}}}
\newcommand{\sat}[1]{\llbracket#1 \rrbracket}

%% Autre
\newcommand{\avance}{{\large$\bigstar$}}

%% Titre
\title{IGL502/752: devoir 1}
\author{Foo McBar}
\date{22 septembre 2020}

\begin{document}
\maketitle

%% Contenu
\section*{Question 1}

\begin{enumerate}[(a)]

\item $\T = (S, \trans{}, I)$ où $S = \{s_0, s_1\}$, $I = \{s_0\}$,
  ...

\item Une structure de Kripke:

  \begin{center}
    \begin{tikzpicture}[auto, node distance=3cm, very thick, initial text=]
      %% États
      \node[state, initial,     label={$\{p\}$}]           (s0) {$s_0$};
      \node[state, right of=s0, label={$\{p, q\}$}]        (s1) {$s_1$};
      \node[state, below of=s0, label={below:$\{q\}$}]     (s2) {$s_2$};
      \node[state, right of=s2, label={below:$\emptyset$}] (s3) {$s_3$};
      \node[state, right of=s3, label={below:$\{q\}$}]     (s4) {$s_4$};

      %% Transitions
      \path[->]
      (s1) edge[loop right]   node {} ()
      (s1) edge[]             node {} (s0)
      (s0) edge[]             node {} (s2)
      (s2) edge[]             node {} (s1)
      (s2) edge[]             node {} (s3)
      (s3) edge[bend left=20] node {} (s4)
      (s4) edge[bend left=20] node {} (s3)
      (s4) edge[loop above]   node {} ()
      (s1) edge[]             node {} (s4)
      ;
    \end{tikzpicture}
  \end{center}

\item[\avance{}] Nous avons:

  \[ p \trans{} q \trans{} r \trans{} \cdots \]

\end{enumerate}

\section*{Question 2}

$\sigma_i \models \varphi_j$ car...

$\{p\}\{p\}\{p, q\}\{r\}^\omega \not\models \varphi_k$ car...

\section*{Question 3}

\begin{enumerate}[(a)]

\item $\neg p \land \F\G((\X p) \U (p \lor q))$

\item $p \rightarrow q$

\item Soit $\sigma \in (2^{AP})^\omega$...

\item ...

\end{enumerate}

\section*{Question 4}

\begin{enumerate}[(a)]

\item $\Pre(s_0) = \emptyset$, $\Post(s_3) = \emptyset$, $\Pre^*(s_1)
  = \emptyset$, $\Post^*(s_4) = \emptyset$

\item Oui, car...

\item ...

\item ...

\end{enumerate}

\section*{Question 5}

\begin{enumerate}[(a)]
  \item $\G(p \rightarrow \X^{-1} q)$

  \item $\G \F^{-1} p$
  
  \item $\F(q \land \G^{-1} (p \lor q))$

  \item $\G(p \rightarrow \F^{-1} q)$
\end{enumerate}

\end{document}
